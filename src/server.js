import express from "express";import {initDb,repos} from "./db.js";import posts from "./routes/posts.js";import variants from "./routes/variants.js";import jobs from "./routes/jobs.js";initDb();const app=express();app.use(express.json({limit:"1mb"}));app.get("/health",(_q,res)=>res.json({ok:true,service:"flyrank-capstone-social-studio"}));app.use("/api/posts",posts);app.use("/api/variants",variants);app.use("/api/jobs",jobs);app.get("/api/publish-attempts",(_q,res)=>res.json(repos.attempts.list.all()));app.get("/api/stats",(_q,res)=>res.json({posts:repos.posts.list.all().length,variants:repos.variants.list.all().length,jobs:repos.jobs.list.all().length,published:repos.attempts.list.all().filter(x=>x.status==="success").length}));app.use((e,_q,res,_n)=>res.status(e.status||(e.name==="ZodError"?400:500)).json({error:e.message,details:e.issues?.map(x=>x.message)}));app.listen(Number(process.env.PORT||3000),()=>console.log(`API on http://localhost:${process.env.PORT||3000}`));
