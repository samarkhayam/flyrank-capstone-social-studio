import {SocialPublisher} from "./socialPublisher.js"; import {config} from "../config.js";
export class TelegramPublisher extends SocialPublisher{async publish({body}){if(!config.telegramBotToken||!config.telegramChatId)throw new Error("Telegram credentials are not configured");const r=await fetch(`https://api.telegram.org/bot${config.telegramBotToken}/sendMessage`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({chat_id:config.telegramChatId,text:body})});if(!r.ok){const e=new Error(`Telegram publish failed: HTTP ${r.status}`);e.retryable=r.status===429||r.status>=500;throw e}const d=await r.json();return{externalId:String(d.result?.message_id??"telegram-message")}}}
